#!/usr/bin/env bash
#docker stop ubuntu-server
#docker rm ubuntu-server
#docker rmi ubuntu-image
#docker build -t ubuntu-image .
#docker run -d --name ubuntu-server ubuntu-image
#docker run -dit --cap-add=NET_ADMIN --cap-add=NET_RAW -p 20022:20022 -p 15034:15034 --name ubuntu-server ubuntu-image
#docker exec -it ubuntu-server bash
